# moon-lit sound pack — audition v1

`moon-lit-v1.wav` · 2:20.5 · 48000 Hz stereo

Generated by `tools/soundpack/render.mjs`. Timestamps below are exact — quote one when something is wrong ("1:12 is too bright") and it can be fixed without re-auditioning the rest.


## 0:00.0 — A · Elements — the raw ingredients

Each is a physical gesture, not a waveform. If one is wrong here, every cue built on it is wrong.

- `0:00.0` strike — the contact click
- `0:01.0` rustle — friction, swept
- `0:02.3` pluck — Karplus–Strong string
- `0:04.8` creak — stick-slip friction
- `0:06.3` droplet — upward sweep
- `0:07.4` body — inharmonic partials
- `0:09.6` thump — low impact

## 0:14.0 — B · Each cue — dry, then in the room

First without reverb, then with. The second is what the shared space contributes; it is why overlaid cues fuse rather than stack.

- `0:14.0` lantern-launch — dry
- `0:15.0` lantern-launch — in the room
- `0:16.0` match — dry
- `0:17.5` match — in the room
- `0:19.1` drop — dry
- `0:19.9` drop — in the room
- `0:20.7` trellis — dry
- `0:22.0` trellis — in the room
- `0:23.3` dead-line-warning — dry
- `0:24.7` dead-line-warning — in the room
- `0:26.2` menu-click — dry
- `0:26.9` menu-click — in the room
- `0:27.6` win — dry
- `0:35.6` win — in the room
- `0:43.7` game-over — dry
- `0:47.4` game-over — in the room

## 0:54.2 — C · Repetition — does it fatigue?

The same cue fired eight times. On the old engine every play was byte-identical; here pitch, strike position and decay all vary per play.

- `0:54.2` match ×8
- `0:59.7` drop ×8
- `1:04.5` menu-click ×8

## 1:11.0 — D · The chain ladder

match at cluster size 3, 4, 5, 6 — the pitch rises with the size of the group that popped.

- `1:11.0` match 3 → 4 → 5 → 6

## 1:18.2 — E · Combinations — cues overlapping as they actually fire

This is the real test. Series and combination, at gameplay density.

- `1:18.2` a modest clear
- `1:22.1` a cascade
- `1:27.3` under pressure

## 1:35.2 — F · Landmarks

The two moments that carry the emotional weight, in full.

- `1:35.2` win — the temple bell
- `1:43.3` game-over — koto

## 1:50.0 — G · The ambient bed

River and irregular taiko — what docs/design-concept.md §8 has asked for since the beginning, and the old engine could not sustain at all. Alone, then underneath gameplay.

- `1:50.0` bed alone
- `2:03.6` bed + gameplay
